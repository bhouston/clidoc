# Commander demo

Run `pnpm --filter @opencli/demo-commander build`, then `pnpm --filter @opencli/demo-commander start -- greet Ada` or `pnpm --filter @opencli/demo-commander start -- --opencli` to print its OpenCLI document.
