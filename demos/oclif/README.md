# oclif demo

Run `pnpm --filter @opencli/demo-oclif build`, then `pnpm --filter @opencli/demo-oclif start -- Ada` or `pnpm --filter @opencli/demo-oclif start -- --opencli` to print its OpenCLI document. The conversion reads the same metadata shape emitted into oclif's `manifest.json`.
