name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited, ready_for_review]
  workflow_call:

permissions:
  contents: read

jobs:
  quality:
    name: Quality (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test:workflow
      - run: pnpm lint
      - run: pnpm tsc
      - name: Install completion test shells (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y zsh fish
      - name: Install completion test shells (macOS)
        if: runner.os == 'macOS'
        run: brew install fish
      - run: pnpm test:coverage
      - run: pnpm audit --audit-level=high
      - run: pnpm package:check
      - run: pnpm docs:build
      - name: Validate generated OpenCLI with the published action
        uses: bhouston/clidoc-action@v1
        with:
          files: docs/generated/clidoc.json
          specification: bcdxn
          format: json
      - uses: actions/upload-artifact@v4
        with:
          name: quality-reports-${{ matrix.os }}
          path: |
            coverage/
      - name: Upload coverage
        if: runner.os == 'macOS'
        uses: codecov/codecov-action@v5
        with:
          files: coverage/lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  website-browser:
    name: Website browser smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: SITE_URL=http://127.0.0.1 BASE_URL=/ pnpm docs:build
      - run: pnpm --filter @clidoc/website exec playwright install --with-deps chromium
      - run: pnpm --filter @clidoc/website test:browser

  policy:
    name: PR policy
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
          fetch-depth: 0
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Validate PR title and workflow
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_BASE: ${{ github.base_ref }}
          PR_HEAD: ${{ github.head_ref }}
          PR_HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          PR_REPO: ${{ github.repository }}
        run: |
          printf '%s\n' "$PR_TITLE" | pnpm exec commitlint
          node scripts/check-pr-policy.mjs
      - name: Lint pull request commits
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: pnpm exec commitlint --from "$BASE_SHA" --to "$HEAD_SHA" --verbose
